re.,name: Update gist
on:
  repository_dispatch:
    types: [build-event]
  schedule:
    - cron: "0 0 * * *"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: 26801518fb6e86b3d16fd7ed445e112d
          TIMEZONE: Asia/Seoul
